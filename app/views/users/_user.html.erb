<li><%= link_to user.last_name + ', ' + user.first_name, user %>
	<% if current_user.admin? && !current_user?(user) %>
	  | <%= link_to t(:delete_link), user, :method => :delete, :data => { :confirm => t(:you_sure) } %>
	<% end %>
</li>